class Coord
    x = none
    y = none

    fun _init (self, X, Y)
        self.x = X
        self.y = Y
    
    fun print (self)
        put ('Coordinates (')
        put (self.x)
        put (', ')
        put (self.y)
        putln (')')
